import cls from './Menu.module.scss'

interface MenuProps {
    className?: string;
}
export const Menu = ({}: MenuProps) => {
    return (
        <div className={cls.Menu}>
            
        </div>
    );
};